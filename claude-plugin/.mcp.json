{
  "mcpServers": {
    "memory": {
      "command": "${HOME}/.memxt/bin/memxt",
      "args": ["mcp"],
      "env": {
        "MEMXT_DB": "${HOME}/.memxt/palace.db",
        "MEMXT_MODEL": "${HOME}/.memxt/lib/minilm.gguf"
      }
    }
  }
}
